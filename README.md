# [Zepto.js](http://zeptojs.com/) – a minimalist JavaScript library

# This is a subset of the original Zepto lib

dist/zepto-[hash].js [zepto, event]
==

Caught You
==

1. no `$el.eq(n)`, `$el.next()`, `$el.prev()`
